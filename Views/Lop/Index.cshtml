@model IEnumerable<DOAN_WEBNC.Models.Lop>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenLop)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLop)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDLop }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDLop }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDLop })
        </td>
    </tr>
}

</table>
